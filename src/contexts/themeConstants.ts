/**
 * Theme Constants
 *
 * Theme-related constants and types for the theme system
 */

export type Theme = 'light' | 'dark' | 'system';

export const THEME_STORAGE_KEY = 'bf-theme';

export const DEFAULT_THEME: Theme = 'system';
