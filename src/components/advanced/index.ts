/**
 * Advanced Features Components
 *
 * Export all advanced feature components for easy importing
 */

export { ThemeSwitcher } from '../ThemeSwitcher';
export { TextSizeControls } from '../TextSizeControls';
export { KeyboardShortcuts } from '../KeyboardShortcuts';
export { ShortcutsHelp } from '../ShortcutsHelp';

// Re-export contexts for convenience
export { ThemeProvider, useTheme } from '../../contexts/ThemeContext';
export { TextSizeProvider, useTextSize } from '../../contexts/TextSizeContext';
